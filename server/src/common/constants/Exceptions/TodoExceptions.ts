export enum TodoExceptionMessage {
  TODO_NOT_FOUND = "Todo not found",
}

export enum TodoSuccessMessage {
  TODO_DELETED = "Todo successfully deleted",
  TODOS_DELETED = "Todos successfully deleted",
}
