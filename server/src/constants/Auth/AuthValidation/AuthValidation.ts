export enum AuthValidation {
  INVALID_PASSWORD = "Invalid password",
  INVALID_VALIDATION = "Validation error",
}

export enum LoginValidationField {
  PASSWORD_MIN_LENGTH = 6,
  PASSWORD_MAX_LENGTH = 16,
}
