export const enum AuthValidateData {
  MAX_PASSWORD_LENGTH = 16,
  MIN_PASSWORD_LENGTH = 6,
}

export const enum AuthValidateMessages {
  INVALID_EMAIL = "Invalid email",
  MAX_LENGTH = "Must be at most 16 characters",
  MIN_LENGTH = "Must be at least 6 characters",
}
